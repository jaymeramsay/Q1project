//Canvas creation
let canvas = document.querySelector('canvas');

canvas.width = '700'; //window.innerWidth stretches to the whole window left/right
canvas.height = '500'; //window.innerHeight stretches to the whole window up/down

let context = canvas.getContext('2d'); //canvas object uses method getContext to access to a lot of drawing info

//Side Bar

let clothingBar = document.getElementById('clothingBar');
let utilityBar = document.getElementById('utilityBar');

//Clothing Div??
